@model IEnumerable<Pages>

@foreach (var item in Model)
{
	@if(item.Slug is not "home")
	{
		<li class=" nav-item">
			<a class=" nav-link text-dark" asp-area="" asp-controller="pages" asp-action="page" asp-route-slug="@item.Slug">
				@item.Title
			</a>
		</li>
	}
	
}

